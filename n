#!/bin/bash
# Quick commit and push for niac-atlas
# Usage: ./b "commit message"

if [ -z "$1" ]; then
  echo "Usage: ./n \"commit message\""
  exit 1
fi

git add . && git commit -m "$1" && git push origin main
